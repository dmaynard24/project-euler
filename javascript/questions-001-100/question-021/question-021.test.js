const getAmicableSum = require('./question-021');

test('gets the sum of all the amicable numbers under 10000 to be 31626', () => {
  expect(getAmicableSum(10000)).toBe(31626);
});

test('gets the sum of all the amicable numbers under 10000 to be 31626', () => {
  expect(getAmicableSum(10000)).toBe(31626);
});
